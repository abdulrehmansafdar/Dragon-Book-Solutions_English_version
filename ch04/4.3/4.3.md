# Exercises in Section 4.3

### 4.3.1

Below is a regular expression grammar containing only the symbols a and b.It uses + instead of the union character | to avoid confusion with the vertical bar used as a metasymbol in the grammar:

rexpr -> rexpr + rterm | rterm
rterm -> rterm rfactor | rfactor
rfactor -> rfactor * | rprimary
rprimary -> a | b

1. Extract the left common factor of this grammar.
2. Can the transformation that extracts the left common factor make this grammar suitable for top-down parsing techniques?
3. After extracting the left common factor, eliminate left recursion from the original grammar.
4. Is the resulting grammar suitable for top-down grammar analysis?

#### Answer

1. No left common factor
2. Not suitable
3. Eliminate left recursion

rexpr -> rterm A
A -> + rterm A | ε
rterm -> rfactor B
B -> rfactor B | ε
rfactor -> rprimary C
C -> * C | ε
rprimary -> a | b

4. Suitable?

### 4.3.2

Repeat exercise 4.3.1 for the following grammar

1. Grammar of Exercise 4.2.1
2. Grammar of Exercise 4.2.2-1
3. Grammar of Exercise 4.2.2-3
4. Grammar of Exercises 4.2.2-5
5. Grammar of Exercise 4.2.2-7

#### Answer

1. S -> S S + | S S * | a
1. Extract the left common factor

S -> S S A | a
A -> + | *

2. Not suitable
3. Eliminate left recursion

// initial status
1) S -> S S A | a
2) A -> + | *

// i = 1
1) S -> a B
2) B -> S A B | ε
3) A -> + | *

// i = 2, j = 1
1) S -> a B
2) B -> a B A B | ε
3) A -> + | *

// i = 3, j = 1 ~ 2
// nothing changed

4. Suitable

2. S -> 0 S 1 | 0 1
1. Extract the left common factor

S -> 0 A
A -> S 1 | 1

2. Not suitable, there is indirect left recursion
3. Eliminate left recursion

// initial status
1) S -> 0 A
2) A -> S 1 | 1

// i = 1
// nothing changed

// i = 2, j = 1
1) S -> 0 A
2) A -> 0 A 1 | 1

4. Suitable

3. S -> S (S) S | ε
1. No left common factor
2. inappropriate
3. Eliminate left recursion

// initial status
1) S -> S (S) S | ε

// i = 1
1) S -> A
2) A -> (S) S A | ε

// i = 2, j = 1
// nothing changed
4. Suitable

4. S -> (L) | a and L -> L, S | S
1. No left common factor
2. inappropriate
3. Eliminate left recursion

// initial status
1) S -> (L) | a
2) L -> L, S | S

// i = 1
// nothing changed

// i = 2, j = 1
1) S -> (L) | a
2) L -> (L) A | a A
3) A -> , S A | ε

// i = 3, j = 1~2
// nothing changed

4. Suitable

### 4.3.3 ！

The purpose of the following grammar is to eliminate the “dangling-else ambiguity” discussed in Section 4.3.2:

stmt -> if expr then stmt
| matchedStmt
matchedStmt -> if expr then matchedStmt else stmt
| other

It shows that this grammar is still ambiguous.

#### Answer

Looking at a sample code, we use indentation to indicate the hierarchical structure of code parsing.

if expr
then
if expr
then matchedStmt
else
if expr
then matchedStmt
else stmt

This code can also be parsed into

if expr
then
if expr
then matchedStmt
else
if expr
then matchedStmt
else stmt

So this is still an ambiguous grammar.The reason is that the last `stmt` in `matchedStmt -> if expr then matchedStmt else stmt`, if it contains an `else` statement, can be considered to belong to this `stmt` or to the statement containing this `matchedStmt`.